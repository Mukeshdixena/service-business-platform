# Base URL the frontend calls for the backend REST API (see docs/API_CONTRACT.md).
# Copy this file to .env (or .env.local) and adjust if your backend runs elsewhere.
VITE_API_BASE_URL=http://localhost:8080/api/v1
